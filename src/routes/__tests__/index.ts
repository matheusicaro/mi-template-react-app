describe("Test 'index.ts'", () => {
  test('Should return expected route order for rendered child components', () => {
    /**
     * TODO: missing test due to lack of developer time
     */
  });
});
